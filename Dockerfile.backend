FROM node:20-alpine

# Build-time deps for better-sqlite3 native module
RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY backend/package*.json ./
RUN npm install --omit=dev

COPY backend/ ./

EXPOSE 3001

CMD ["node", "server.js"]
